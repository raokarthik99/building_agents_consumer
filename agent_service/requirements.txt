# High-level dependencies (unpinned). After edits run:
#   pip install -r requirements.txt
#   pip freeze --exclude-editable > requirements.lock
fastapi
uvicorn[standard]
python-dotenv
pydantic
google-adk
google-genai
ag-ui-adk
composio
httpx
PyJWT
litellm
